from __future__ import annotations

DIFFICULTY_RULES = {
    "easy": (
        "Простой запрос: одна таблица, выбор нескольких полей, простые фильтры, "
        "сортировка, LIMIT или простая агрегация без сложных соединений."
    ),
    "medium": (
        "Средний запрос: соединение 2-3 таблиц, группировки, агрегаты, фильтры по датам, "
        "условные выражения или расчетные поля."
    ),
    "hard": (
        "Сложный запрос: CTE, подзапросы, оконные функции, несколько соединений, "
        "многошаговая бизнес-логика или сравнение агрегированных результатов."
    ),
}

QUERY_DESCRIPTION_REWRITE_STYLES = {
    "short": (
        "Короткий стиль, согласованный с краткими комментариями в схеме: одно сжатое предложение "
        "в формате «Скрипт для …». Используй минимум слов, без пояснения бизнес-контекста и без "
        "перечисления технических деталей реализации. Называй сущности так, как они названы "
        "в комментариях схемы (кратко: «клиент», «платёж», «магазин»), а не развёрнутыми "
        "определениями. Если в схеме таблицы и поля обозначены короткими кодами (act, cus, pay, "
        "a01, h02), допустимо ссылаться на них вместо длинных описаний."
    ),
    "business": (
        "Бизнес-стиль, согласованный с развёрнутыми комментариями в схеме: опиши операционную "
        "цель, предметную область и смысл анализа так, как это следует из бизнес-комментариев "
        "к таблицам и полям. Используй формулировки о назначении данных, связях между сущностями "
        "и практическом применении отчёта, которые явно или неявно присутствуют в комментариях "
        "схемы. Не перечисляй технические коды полей и не описывай пошаговый алгоритм SQL."
    ),
    "technical": (
        "Технический стиль, согласованный с лаконичной схемой с кодами таблиц и полей: опиши "
        "задачу через структуру данных — какие таблицы задействованы (по кодам из DDL, "
        "например act, cus, pay), какие поля нужны, какие фильтры, группировки, сортировки, "
        "агрегации и соединения предполагаются. Формулировка должна читаться как спецификация "
        "запроса к конкретной схеме, а не как бизнес-требование пользователя."
    ),
}


def build_query_description_prompt(
    schema_description: str,
    difficulty: str,
    database_name: str,
) -> list[dict[str, str]]:
    difficulty_rule = _difficulty_rule(difficulty)
    return [
        {
            "role": "system",
            "content": (
                "Ты помогаешь подготовить датасет для обучения text-to-SQL системы. "
                "Все ответы должны быть на русском языке."
            ),
        },
        {
            "role": "user",
            "content": (
                f"База данных: {database_name}\n\n"
                "Ниже приведена схема базы данных с комментариями и примерами первых строк:\n"
                f"{schema_description}\n\n"
                "Нужно придумать ровно одно описание аналитического SQL-запроса на естественном "
                "русском языке.\n"
                f"Сложность: {difficulty}\n"
                f"Правила сложности: {difficulty_rule}\n\n"
                "Требования:\n"
                "- Верни только одно описание запроса.\n"
                "- Не пиши SQL-код.\n"
                "- Не добавляй нумерацию, markdown, пояснения или кавычки.\n"
                "- Описание должно быть реалистичным для банковского мониторинга транзакций."
            ),
        },
    ]


def build_sql_generation_prompt(
    schema_description: str,
    query: str,
    difficulty: str,
) -> list[dict[str, str]]:
    difficulty_rule = _difficulty_rule(difficulty)
    return [
        {
            "role": "system",
            "content": (
                "Ты эксперт по SQLite. Генерируй только безопасные read-only SQL-запросы. "
                "Не изменяй данные и не используй DDL/DML."
            ),
        },
        {
            "role": "user",
            "content": (
                "Ниже приведена схема базы данных с комментариями и примерами первых строк:\n"
                f"{schema_description}\n\n"
                f"Описание запроса: {query}\n"
                f"Сложность: {difficulty}\n"
                f"Правила сложности: {difficulty_rule}\n\n"
                "Сгенерируй SQL для SQLite.\n"
                "Требования:\n"
                "- Верни только SQL-код, без пояснений.\n"
                "- Используй только таблицы и поля из схемы.\n"
                "- Запрос должен быть read-only и возвращать строки.\n"
                "- Если используешь markdown, помести SQL только в блок ```sql."
            ),
        },
    ]


def build_related_query_description_prompt(
    schema_description: str,
    query_description: str,
    style: str,
    difficulty: str | None = None,
) -> list[dict[str, str]]:
    style_rule = _rewrite_style_rule(style)
    schema_style_guidance = _schema_driven_style_guidance(style)
    difficulty_text = ""
    if difficulty is not None:
        difficulty_text = (
            f"\nСложность исходного запроса: {difficulty}\n"
            f"Правила сложности: {_difficulty_rule(difficulty)}"
        )

    return [
        {
            "role": "system",
            "content": (
                "Ты помогаешь расширять датасет для text-to-SQL системы, создавая "
                "связанные вариации описаний аналитических запросов. "
                "Стиль каждого описания должен определяться прежде всего тем, как "
                "представлена схема базы данных: длина и характер комментариев к таблицам "
                "и полям, наличие бизнес-контекста или технических кодов. "
                "Все ответы должны быть на русском языке."
            ),
        },
        {
            "role": "user",
            "content": (
                "Ниже приведена схема базы данных с комментариями и примерами первых строк. "
                "Именно эта схема задаёт нужный уровень детализации и лексику для результата:\n"
                f"{schema_description}\n\n"
                f"Исходное описание запроса: {query_description}"
                f"{difficulty_text}\n"
                f"Целевой стиль: {style}\n"
                f"Правила стиля: {style_rule}\n"
                f"Как стиль связан со схемой: {schema_style_guidance}\n\n"
                "Придумай новое описание аналитического запроса, которое:\n"
                "- остаётся семантически связанным с исходным (та же предметная область, "
                "похожие таблицы и тип анализа);\n"
                "- не является дословной копией исходного описания;\n"
                "- допускает изменение лимитов, фильтров, группировок или смежный "
                "аналитический угол в рамках той же задачи;\n"
                "- явно отличается по тону и детализации от описаний в других стилях "
                "(short / business / technical), потому что опирается на комментарии "
                "именно этой схемы.\n\n"
                "Требования:\n"
                "- Верни ровно одно новое описание запроса в указанном стиле.\n"
                "- Сохрани уровень сложности исходного запроса.\n"
                "- Используй терминологию, таблицы и поля из приложенной схемы; "
                "не подставляй названия из другой версии схемы.\n"
                "- Не пиши SQL-код.\n"
                "- Не добавляй markdown, нумерацию, кавычки или пояснения."
            ),
        },
    ]


def build_query_description_rewrite_prompt(
    schema_description: str,
    query_description: str,
    rewrite_style: str,
    difficulty: str | None = None,
) -> list[dict[str, str]]:
    style_rule = _rewrite_style_rule(rewrite_style)
    schema_style_guidance = _schema_driven_style_guidance(rewrite_style)
    difficulty_text = ""
    if difficulty is not None:
        difficulty_text = f"\nСложность исходного запроса: {difficulty}"

    return [
        {
            "role": "system",
            "content": (
                "Ты редактируешь описания SQL-скриптов для text-to-SQL датасета. "
                "Результат должен звучать как описание алгоритма или назначения скрипта, "
                "а не как пользовательская команда. "
                "Стиль переписанного описания определяется прежде всего приложенной "
                "схемой базы данных: используй тот же уровень детализации, ту же лексику "
                "и тот же способ ссылаться на таблицы и поля, который задан комментариями "
                "в этой схеме. Все ответы должны быть на русском языке."
            ),
        },
        {
            "role": "user",
            "content": (
                "Ниже приведена схема базы данных с комментариями и примерами первых строк. "
                "Переписанное описание должно быть согласовано именно с этой схемой, "
                "а не с абстрактными правилами форматирования:\n"
                f"{schema_description}\n\n"
                f"Исходное описание запроса: {query_description}"
                f"{difficulty_text}\n"
                f"Целевой стиль: {rewrite_style}\n"
                f"Правила стиля: {style_rule}\n"
                f"Как стиль связан со схемой: {schema_style_guidance}\n\n"
                "Преобразуй исходное описание в задачу скрипта:\n"
                "- Убери императивные формулировки («Показать», «Вывести», «Рассчитать»).\n"
                "- Начни с «Скрипт для …», «Алгоритм …», «Расчёт …» или «Поиск …», "
                "если это звучит естественно для выбранного стиля.\n"
                "- Сохрани аналитический смысл и уровень сложности.\n"
                "- Сделай текст заметно отличным от версий в других стилях за счёт того, "
                "что short опирается на краткие комментарии схемы, business — на "
                "развёрнутый предметный контекст, technical — на коды таблиц/полей и "
                "структуру запроса.\n\n"
                "Требования:\n"
                "- Верни ровно одно переписанное описание.\n"
                "- Используй терминологию из приложенной схемы; не выдумывай названия "
                "полей и таблиц, которых нет в DDL.\n"
                "- Не пиши SQL-код.\n"
                "- Не добавляй markdown, нумерацию, кавычки или пояснения."
            ),
        },
    ]


def _difficulty_rule(difficulty: str) -> str:
    try:
        return DIFFICULTY_RULES[difficulty]
    except KeyError as exc:
        supported = ", ".join(sorted(DIFFICULTY_RULES))
        raise ValueError(f"Unknown difficulty={difficulty!r}. Supported: {supported}.") from exc


def _rewrite_style_rule(rewrite_style: str) -> str:
    try:
        return QUERY_DESCRIPTION_REWRITE_STYLES[rewrite_style]
    except KeyError as exc:
        supported = ", ".join(sorted(QUERY_DESCRIPTION_REWRITE_STYLES))
        raise ValueError(
            f"Unknown rewrite_style={rewrite_style!r}. Supported: {supported}."
        ) from exc


SCHEMA_DRIVEN_STYLE_GUIDANCE = {
    "short": (
        "В приложенной схеме комментарии к таблицам и полям короткие и предметные. "
        "Переписанное описание должно повторять эту лаконичность: одна мысль, без "
        "развёрнутых пояснений и без перечисления шагов. Бери названия сущностей "
        "из комментариев схемы дословно или почти дословно. Если в DDL таблицы "
        "имеют короткие коды (act, cus, pay), а поля — непрозрачные идентификаторы "
        "(a01, h02), используй их вместо длинных перефразировок."
    ),
    "business": (
        "В приложенной схеме комментарии развёрнутые и объясняют бизнес-назначение "
        "таблиц, связи между сущностями и практическое применение данных. "
        "Переписанное описание должно звучать как постановка аналитической задачи "
        "для бизнес-пользователя: зачем нужен отчёт, какие процессы или решения "
        "он поддерживает, какие сущности и показатели затрагивает. Опирайся на "
        "формулировки из комментариев схемы, а не на технические коды полей."
    ),
    "technical": (
        "В приложенной схеме комментарии минимальны, а таблицы и поля обозначены "
        "короткими кодами. Переписанное описание должно читаться как техническая "
        "спецификация запроса: перечисли задействованные таблицы по кодам из DDL, "
        "укажи нужные поля, фильтры, группировки, сортировки, агрегации и соединения. "
        "Не заменяй коды таблиц и полей разговорными синонимами, если они явно "
        "присутствуют в схеме."
    ),
}


def _schema_driven_style_guidance(style: str) -> str:
    try:
        return SCHEMA_DRIVEN_STYLE_GUIDANCE[style]
    except KeyError as exc:
        supported = ", ".join(sorted(SCHEMA_DRIVEN_STYLE_GUIDANCE))
        raise ValueError(f"Unknown style={style!r}. Supported: {supported}.") from exc
